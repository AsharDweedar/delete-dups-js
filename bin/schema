const mongoose = require('./db');
const Schema = mongoose.Schema;

const mySchema = new Schema({
 _id: Schema.ObjectId,
 paths: Array,
 hash: String
});

module.exports = mongoose.model('paths', mySchema);

